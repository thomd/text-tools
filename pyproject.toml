[project]
name = "text-tools"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["nltk"]

[project.scripts]
text = "text_tools.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/text_tools"]
